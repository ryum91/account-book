<?xml version="1.0" encoding="UTF-8"?>
<configuration xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://www.padual.com/java/logback.xsd">
	<statusListener class="ch.qos.logback.core.status.NopStatusListener" />
	<appender name="logfile" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>../logs/accountbook.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
			<fileNamePattern>../logs/accountbook_old_%i.log</fileNamePattern>
			<maxIndex>2</maxIndex>
		</rollingPolicy>
		<triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
			<maxFileSize>50MB</maxFileSize>
		</triggeringPolicy>
		<append>true</append>
		<encoder>
			<Pattern>[%d{YYYY-MM-dd HH:mm:ss}] [%-5p] [%F:%L] %m%n</Pattern>
		</encoder>
	</appender>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>[%d{YYYY-MM-dd HH:mm:ss}] [%-5p] [%F:%L] %m%n</Pattern>
        </layout>
    </appender>
    
    <!-- TRACE | DEBUG | INFO | WARN | ERROR -->
 
 	<!-- Spring Framework -->
    <logger name="org.springframework" level="ERROR" />
 	
 	<!-- com.ryum -->
    <logger name="com.ryum" level="WARN" />
    
    <!-- ROOT -->
    <if condition='p("spring.profiles.active").equals("prod")'>
    	<then>
			<root level="WARN">
		        <appender-ref ref="logfile" />
		    </root>    	
    	</then>
    	<else>
			<root level="WARN">
		        <appender-ref ref="console" />
		    </root>    	
    	</else>
    </if>
</configuration>